// ============================================================
// index.js — يشغّل جسر واتساب + عامل الذكاء في عملية واحدة
// ============================================================

console.log('🚀 تشغيل تيسير: واتساب ⇄ Firestore ⇄ Groq');

require('./server');

try {
  require('./ai-worker');
} catch (e) {
  console.error('⚠️ فشل تشغيل عامل الذكاء، سيستمر جسر واتساب بالحفظ:', e.message);
}
